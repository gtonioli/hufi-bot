{
  "name": "hufi-bot",
  "version": "1.0.0",
  "scripts": {
    "start": "node node_modules/babel-cli/bin/babel src -d dist --source-maps && node dist/server.js",
    "build": "node node_modules/babel-cli/bin/babel src -d dist --source-maps"
  },
  "dependencies": {
    "axios": "^0.18.0",
    "discord.js": "^11.3.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
